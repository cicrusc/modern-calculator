/*class CalculatorBrainTest {
    private val brain = CalculatorBrain()

    @Test
    fun testAddition() {
        brain.processInput("5")
        brain.processInput("+")
        brain.processInput("3")
        assertEquals("8", brain.processInput("="))
    }

    @Test
    fun testDivisionByZero() {
        brain.processInput("5")
        brain.processInput("÷")
        brain.processInput("0")
        assertEquals("Errore", brain.processInput("="))
    }
}*/